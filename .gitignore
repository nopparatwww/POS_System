############################
# Root .gitignore (repo)   #
############################

# Node.js dependencies
node_modules/
Backend/node_modules/
Frontend/node_modules/

# Environment files (never commit secrets)
.env
.env.*
.env.local
Backend/.env
Backend/.env.*
Backend/.env.local
Frontend/pos-system/.env
Frontend/pos-system/.env.*
Frontend/pos-system/.env.local
# Keep examples tracked if present
!.env.example
!Backend/.env.example
!Frontend/pos-system/.env.example

# Logs
# Keep only the ROOT /logs folder ignored; do not ignore nested folders named "logs" in src.
# Allow a placeholder to keep the root folder tracked if needed.
/logs/*
!/logs/.gitkeep
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build output / bundles
dist/
build/
**/dist-ssr/
.vite/

# Test coverage
coverage/
coverage-final.json

# OS files
.DS_Store
Thumbs.db

# IDE/project files
.vscode/
.idea/
.history/
*.iml

# Misc caches
.cache/
.parcel-cache/
.turbo/

# Windows specific
*.log
*.tmp
*.bak

# Lock files (recommend committing, so left commented)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Nested repo backup
POS_System/.git.bak_*
